# 数据集选择与使用的关键经验

**创建时间**: 2024-11-05
**适用场景**: SmolVLA数据集选择、数据准备、归一化处理

---

## 一、SmolVLA适用数据集总结

### 1.1 已训练过的数据集（可直接使用或微调）

✅ **推荐使用的数据集**：

| 数据集 | 成功率 | 任务类型 | 环境 | 推荐度 |
|--------|--------|----------|------|--------|
| **LIBERO** | 82% | 130个语言条件任务 | 仿真 | ⭐⭐⭐⭐⭐ |
| **SO-100** | 78% | 真实机器人操作 | 真实 | ⭐⭐⭐⭐ |
| **Meta-World** | 87% | 50个多任务 | 仿真 | ⭐⭐⭐⭐⭐ |

**数据集特点**：

**LIBERO**：
- 学术标准基准
- 任务多样性高（130个任务）
- 可以对比论文性能
- 适合学术研究和面试展示

**SO-100**：
- 真实机器人数据（来自Open-X-Embodiment）
- 更接近实际应用
- 测试泛化能力
- 适合真实场景验证

**Meta-World**：
- 成功率最高（87%）
- 安装简单（`pip install metaworld`）
- 任务定义清晰
- 适合快速验证模型工作正常

---

### 1.2 未训练的数据集（不推荐）

❌ **不要使用的数据集**：

| 数据集 | 成功率 | 原因 | 影响 |
|--------|--------|------|------|
| **PushT** | 0% | 2D像素任务，与3D机械臂不匹配 | 浪费时间 |
| **自定义简化环境** | 0% | Domain Gap太大 | 误导性结果 |

**失败案例：PushT数据集**

问题表现：
```
预测值范围: [-1, 1] (归一化)
真实值范围: [70, 230] 像素坐标
直接比较MAE: 232像素 ❌
```

根本原因：
- SmolVLA未在PushT上训练
- PushT是2D推块任务
- SmolVLA训练的都是3D机械臂任务

---

### 1.3 数据集选择原则

**核心原则**: 使用模型训练过的数据集

**正确流程**：
```
1. 查阅论文/文档确认训练数据
2. 选择已训练过的数据集
3. 如需新任务，在你的数据上微调
```

**错误流程**：
```
❌ 随便选个数据集
❌ 期望模型在任何数据集都工作
❌ 不匹配时过度调试
```

---

## 二、归一化的重要性 ⭐⭐⭐⭐⭐

### 2.1 为什么归一化至关重要？

**问题案例：SO100数据集测试**

```python
# 第一次测试（未反归一化）
预测值范围: [-1.63, 3.91]  # 归一化值
真实值范围: [-0.70, 177.19]  # 原始角度
MAE: 80.29°  ❌ 巨大误差！

# 修复后（添加反归一化）
预测值范围: [1.57, 162.72]  # 反归一化后
真实值范围: [-0.70, 177.19]  # 原始角度
MAE: 30.42°  ✅ 误差降低62%！
```

**数值对比表**：

| 样本 | 预测（归一化） | 真实值 | 误差 | 预测（反归一化） | 误差 |
|------|---------------|--------|------|-----------------|------|
| 0 | 0.19 | 233.0 | 232.8 ❌ | 233.5 | 0.5 ✅ |
| 1 | 0.21 | 71.0 | 70.8 ❌ | 71.2 | 0.2 ✅ |

---

### 2.2 归一化原理

**为什么VLA/RL模型使用归一化？**

1. **不同关节角度范围差异大**
   ```
   关节0: 0-180°
   关节1: 56-123°
   关节2: 40-164°
   ...
   ```

2. **归一化使训练稳定**
   - 梯度更新平衡
   - 学习率更容易调整
   - 收敛更快

3. **模型输出在固定范围**
   - 通常 [-1, 1] 或 [0, 1]
   - 方便使用tanh/sigmoid激活

---

### 2.3 正确的归一化方法

**公式**：
```python
# 归一化（训练时）
normalized = (value - mean) / std

# 反归一化（推理时）
denormalized = normalized * std + mean
```

**完整示例代码**：

```python
import numpy as np
from lerobot.datasets.lerobot_dataset import LeRobotDataset

# 1. 加载数据集
dataset = LeRobotDataset("lerobot/svla_so100_pickplace")

# 2. 获取归一化统计信息
action_stats = dataset.meta.stats['action']
action_mean = action_stats['mean']  # numpy array
action_std = action_stats['std']    # numpy array

print(f"均值: {action_mean}")
print(f"标准差: {action_std}")
print(f"范围: {action_stats['min']} ~ {action_stats['max']}")

# 输出示例：
# 均值: [14.51, 146.45, 143.32, 62.96, 85.83, 7.78]
# 标准差: [27.99, 34.99, 21.46, 16.91, 12.48, 9.55]

# 3. 模型推理
pred_norm = policy.select_action(batch)  # 归一化的预测

# 4. 反归一化
pred_denorm = pred_norm * action_std + action_mean

# 5. 与真实值比较（必须同尺度！）
true_action = sample['action']  # 已经是原始尺度
mae = np.mean(np.abs(pred_denorm - true_action))

print(f"MAE: {mae:.2f}°")
```

---

### 2.4 常见错误

**错误1：直接比较不同尺度**
```python
# ❌ 错误
pred = policy.select_action(batch)  # 归一化值
true = sample['action']              # 原始值
mae = np.mean(np.abs(pred - true))   # 尺度不匹配！
```

**错误2：忘记查看数据集统计信息**
```python
# ❌ 错误
# 不知道数据集的mean和std就开始计算

# ✅ 正确
# 先查看 dataset.meta.stats
```

**错误3：使用错误的统计信息**
```python
# ❌ 错误
# 使用observation的统计信息反归一化action
obs_stats = dataset.meta.stats['observation.state']

# ✅ 正确
action_stats = dataset.meta.stats['action']
```

---

### 2.5 检查清单

在评估模型前，确认：

- [ ] 已获取数据集的统计信息（mean, std）
- [ ] 理解预测值的尺度（归一化 or 原始）
- [ ] 理解真实值的尺度（归一化 or 原始）
- [ ] 比较前已统一尺度
- [ ] 打印数值范围验证（pred范围应与true相近）

**验证代码**：
```python
print(f"预测值范围: [{pred_denorm.min():.2f}, {pred_denorm.max():.2f}]")
print(f"真实值范围: [{true_action.min():.2f}, {true_action.max():.2f}]")

# 预期：两个范围应该相近
# 如果差异巨大（如 [-1, 1] vs [0, 180]），说明有问题
```

---

## 三、数据集格式要点

### 3.1 LeRobot数据集标准格式

**目录结构**：
```
demo_data_language/
├── data/
│   └── chunk-000/
│       ├── episode_000000.parquet
│       ├── episode_000001.parquet
│       └── ...
└── meta/
    ├── episodes.jsonl   # episode元数据
    ├── tasks.jsonl      # 任务描述
    └── stats.json       # ⭐ 统计信息（归一化参数）
```

---

### 3.2 关键字段说明

**标准字段表**：

| 字段名 | 维度 | 数据类型 | 说明 | 示例值 |
|--------|------|----------|------|--------|
| `observation.image` | (256,256,3) | uint8 | 主相机RGB图像 | [0-255] |
| `observation.wrist_image` | (256,256,3) | uint8 | 腕部相机图像（可选） | [0-255] |
| `observation.state` | (6,) | float32 | 当前关节角度 | [0.38, -0.11, ...] |
| `action` | (7,) | float32 | 目标动作（6关节+夹爪） | [0.40, -0.11, ..., 0] |
| `task` | string | str | 语言指令 | "Pick red cup" |
| `timestamp` | scalar | float64 | 时间戳（秒） | 0.0, 0.05, 0.10, ... |
| `episode_index` | scalar | int64 | episode编号 | 0, 1, 2, ... |
| `frame_index` | scalar | int64 | 帧编号（episode内） | 0, 1, 2, ... |

**重要区别**：

```python
# state: 当前时刻 t 的实际状态
observation.state[t] = [j0, j1, j2, j3, j4, j5]

# action: 下一时刻 t+1 的目标状态
action[t] = [j0', j1', j2', j3', j4', j5', gripper]

# 训练目标：
# 学习从 (state_t, image_t, task) 预测 action_t
```

---

### 3.3 stats.json内容

**统计信息示例**（SO100数据集）：

```json
{
  "action": {
    "mean": [14.51, 146.45, 143.32, 62.96, 85.83, 7.78],
    "std": [27.99, 34.99, 21.46, 16.91, 12.48, 9.55],
    "min": [-37.18, 48.87, 40.96, 9.67, 56.25, 0.00],
    "max": [72.77, 179.47, 164.62, 96.59, 123.57, 34.93]
  },
  "observation.state": {
    "mean": [14.51, 146.45, 143.32, 62.96, 85.83, 7.78],
    "std": [27.99, 34.99, 21.46, 16.91, 12.48, 9.55],
    "min": [-37.18, 48.87, 40.96, 9.67, 56.25, 0.00],
    "max": [72.77, 179.47, 164.62, 96.59, 123.57, 34.93]
  }
}
```

**读取方法**：
```python
import json

# 方法1：通过LeRobotDataset
from lerobot.datasets.lerobot_dataset import LeRobotDataset
dataset = LeRobotDataset("lerobot/svla_so100_pickplace")
stats = dataset.meta.stats

# 方法2：直接读取JSON
with open('demo_data_language/meta/stats.json', 'r') as f:
    stats = json.load(f)

action_mean = np.array(stats['action']['mean'])
action_std = np.array(stats['action']['std'])
```

---

### 3.4 采样频率

**时间参数**：
```
FPS: 20帧/秒
时间间隔: 0.05秒/帧
控制频率: 20 Hz
```

**时间戳示例**：
```
frame 0: timestamp = 0.00s
frame 1: timestamp = 0.05s
frame 2: timestamp = 0.10s
...
frame 20: timestamp = 1.00s (1秒)
```

---

### 3.5 适配Parol6的修改

**如果你要创建Parol6数据集**：

```python
# 修改特征定义
features = {
    "observation.image": (256, 256, 3),           # 保持不变
    "observation.wrist_image": (256, 256, 3),     # 可选
    "observation.state": (6,),                     # Parol6的6个关节
    "action": (7,),                                # 6关节 + 1夹爪
    "task": "string",                              # 语言指令
}

# 数据采集时确保
- 关节角度单位：弧度（radians）
- 图像尺寸：256x256
- 采样频率：20Hz（推荐）
- 每个episode长度：10-30秒
```

---

## 四、数据质量检查

### 4.1 数据集验证清单

**加载后立即检查**：

```python
from lerobot.datasets.lerobot_dataset import LeRobotDataset

dataset = LeRobotDataset("your_dataset_name")

# 检查1：数据集大小
print(f"总样本数: {len(dataset)}")
print(f"Episode数: {dataset.meta.episodes}")

# 检查2：数据范围
sample = dataset[0]
print(f"图像形状: {sample['observation.image'].shape}")
print(f"状态形状: {sample['observation.state'].shape}")
print(f"动作形状: {sample['action'].shape}")

# 检查3：数值范围
print(f"图像范围: [{sample['observation.image'].min()}, "
      f"{sample['observation.image'].max()}]")  # 应该是[0, 255]
print(f"状态范围: [{sample['observation.state'].min():.2f}, "
      f"{sample['observation.state'].max():.2f}]")

# 检查4：统计信息存在
assert 'action' in dataset.meta.stats
assert 'mean' in dataset.meta.stats['action']
assert 'std' in dataset.meta.stats['action']
print("✅ 统计信息检查通过")
```

---

### 4.2 可视化验证

**图像检查**：
```python
import matplotlib.pyplot as plt

# 显示第一个样本的图像
sample = dataset[0]
plt.figure(figsize=(12, 4))

plt.subplot(131)
plt.imshow(sample['observation.image'].permute(1,2,0))
plt.title("Main Camera")

if 'observation.wrist_image' in sample:
    plt.subplot(132)
    plt.imshow(sample['observation.wrist_image'].permute(1,2,0))
    plt.title("Wrist Camera")

plt.subplot(133)
plt.plot(sample['observation.state'].numpy())
plt.title("Joint States")
plt.xlabel("Joint Index")
plt.ylabel("Angle (rad)")

plt.tight_layout()
plt.savefig("dataset_sample.png")
print("✅ 可视化已保存")
```

**轨迹检查**：
```python
# 检查episode轨迹的连贯性
episode_0 = []
for i in range(100):  # 假设episode 0有100帧
    sample = dataset[i]
    if sample['episode_index'] == 0:
        episode_0.append(sample['observation.state'].numpy())
    else:
        break

trajectory = np.array(episode_0)

plt.figure(figsize=(12, 4))
for j in range(6):
    plt.plot(trajectory[:, j], label=f'Joint {j}')
plt.xlabel("Frame")
plt.ylabel("Joint Angle (rad)")
plt.legend()
plt.title("Episode 0 Trajectory")
plt.savefig("trajectory_check.png")
print("✅ 轨迹可视化已保存")
```

---

## 五、经验总结

### 5.1 关键要点

1. **数据集选择**
   - ✅ 使用模型训练过的数据集
   - ❌ 避免不匹配的数据集

2. **归一化处理**
   - ✅ 必须反归一化后再比较
   - ✅ 查看dataset.meta.stats
   - ❌ 不要直接比较不同尺度

3. **数据格式**
   - ✅ 理解state vs action的区别
   - ✅ 确保维度正确
   - ✅ 检查数值范围合理

4. **数据质量**
   - ✅ 可视化验证
   - ✅ 检查轨迹连贯性
   - ✅ 确保统计信息存在

---

### 5.2 快速诊断表

| 症状 | 可能原因 | 解决方案 |
|------|----------|----------|
| MAE非常大（>100） | 未反归一化 | 检查是否使用stats |
| 预测值范围[-1,1] | 归一化输出 | 反归一化 |
| 成功率0% | 数据集不匹配 | 换用训练过的数据集 |
| 图像显示异常 | 归一化范围错误 | 检查[0,255] vs [0,1] |
| KeyError | 字段名错误 | 检查数据集实际字段 |

---

### 5.3 推荐流程

**新数据集测试流程**：

```
1. 查阅论文确认是否训练过 ✓
   ↓
2. 加载数据集并检查格式 ✓
   ↓
3. 查看stats.json统计信息 ✓
   ↓
4. 可视化样本验证 ✓
   ↓
5. 测试推理（小样本） ✓
   ↓
6. 反归一化预测值 ✓
   ↓
7. 计算指标并分析 ✓
```

---

## 六、参考资料

**数据集资源**：
- LeRobot数据集：https://huggingface.co/lerobot
- LIBERO：https://github.com/Lifelong-Robot-Learning/LIBERO
- Meta-World：https://github.com/Farama-Foundation/Metaworld
- SO100数据：https://huggingface.co/datasets/lerobot/svla_so100_pickplace

**相关文档**：
- `README_DATASET.md` - 数据集下载指南
- `SMOLVLA_DATASETS.md` - SmolVLA训练数据集说明
- `10-SO100测试结果总结.md` - SO100测试报告
- `07-测试结果分析与问题总结.md` - 归一化问题分析

---

**创建时间**: 2024-11-05
**最后更新**: 2024-11-05
**状态**: ✅ 完整
